# 一、六轴陀螺仪加速度传感器

六轴陀螺仪加速度传感器包含3轴陀螺仪（角速度传感器）和3轴重力加速度传感器。经常被用于无人机/四旋翼的姿态解算传感器。本文所介绍的姿态解算也是基于这种传感器值的。常见的六轴陀螺仪加速度传感器有MPU6050，ICM-20602。

# 二、姿态解算

姿态解算是指通过陀螺仪和加速度传感器获取到的角速度和加速度数据，计算出物体在三维空间中的姿态（即物体的旋转状态）。常见的姿态表示方法有欧拉角、四元数、旋转矩阵等。一般常用欧拉角。

# 三、由角速度求解欧拉角的过程

这里采用四元数法由角速度求解欧拉角，总共分为两个步骤

1. 由陀螺仪获取到的角速度数据，求解出四元数
2. 将四元数转换成旋转矩阵
3. 由旋转矩阵求解出欧拉角

## 3.1 由陀螺仪获取到的角速度数据，求解出四元数

这里，采用一阶龙格-库塔法（Euler method）来求解四元数。

一阶龙格-库塔法的核心思想是：在每个时间步长内，假设角速度保持不变，然后根据当前的四元数和角速度来计算下一时刻的四元数。这里直接给出结论，推导过程省略。

<div align="center">
    <img src="https://cdn.jsdelivr.net/gh/lcekold/blogimage@main/Network/20260322013457844.png">
</div>

其中：

<img src="https://cdn.jsdelivr.net/gh/lcekold/blogimage@main/Network/20260322013726385.png">

<img src="https://cdn.jsdelivr.net/gh/lcekold/blogimage@main/Network/20260322013749956.png">

代入可得：

<img src="https://cdn.jsdelivr.net/gh/lcekold/blogimage@main/Network/20260322013818084.png">

其中：

1. 各q初始值，$q_0 = 1,\ q_1 = 0,\ q_2 = 0,\ q_3 = 0$
2. $\omega_x,\ \omega_y,\ \omega_z$分别为陀螺仪获取到的绕x轴、y轴、z轴的角速度值
3. T为采样周期

代码表示：

```c
/* 一阶近似算法，四元数运动学方程的离散化形式和积分 */
    float q0Last = mahony_filter->q0;
    float q1Last = mahony_filter->q1;
    float q2Last = mahony_filter->q2;
    float q3Last = mahony_filter->q3;
    float halfT = mahony_filter->dt * 0.5f;
    mahony_filter->q0 += (-q1Last * mahony_filter->gyro.x - q2Last * mahony_filter->gyro.y - q3Last * mahony_filter->gyro.z) * halfT;
    mahony_filter->q1 += ( q0Last * mahony_filter->gyro.x + q2Last * mahony_filter->gyro.z - q3Last * mahony_filter->gyro.y) * halfT;
    mahony_filter->q2 += ( q0Last * mahony_filter->gyro.y - q1Last * mahony_filter->gyro.z + q3Last * mahony_filter->gyro.x) * halfT;
    mahony_filter->q3 += ( q0Last * mahony_filter->gyro.z + q1Last * mahony_filter->gyro.y - q2Last * mahony_filter->gyro.x) * halfT;
```

## 3.2 四元数与旋转矩阵公式转换

四元数和旋转矩阵之间可以相互转换，公式如下：

$$
\boldsymbol{R} = \begin{bmatrix}
1 - 2(q_2^2 + q_3^2) & 2(q_1q_2 - q_0q_3) & 2(q_1q_3 + q_0q_2) \\
2(q_1q_2 + q_0q_3) & 1 - 2(q_1^2 + q_3^2) & 2(q_2q_3 - q_0q_1) \\
2(q_1q_3 - q_0q_2) & 2(q_2q_3 + q_0q_1) & 1 - 2(q_1^2 + q_2^2)
\end{bmatrix}
$$

显然，旋转矩阵需要九个参数，相比于四元数，参数量更多。但相比于四元数，Rotation Matrix好处是更直观，代表着机体坐标系的三个轴的方向。

通过四元数，计算旋转矩阵的代码如下：

```c
/*计算旋转矩阵*/
void RotationMatrix_update(struct MAHONY_FILTER_t *mahony_filter)
{
    float q1q1 = mahony_filter->q1 * mahony_filter->q1;
    float q2q2 = mahony_filter->q2 * mahony_filter->q2;
    float q3q3 = mahony_filter->q3 * mahony_filter->q3;

    float q0q1 = mahony_filter->q0 * mahony_filter->q1;
    float q0q2 = mahony_filter->q0 * mahony_filter->q2;
    float q0q3 = mahony_filter->q0 * mahony_filter->q3;
    float q1q2 = mahony_filter->q1 * mahony_filter->q2;
    float q1q3 = mahony_filter->q1 * mahony_filter->q3;
    float q2q3 = mahony_filter->q2 * mahony_filter->q3;

    mahony_filter->rMat[0][0] = 1.0f - 2.0f * q2q2 - 2.0f * q3q3;
    mahony_filter->rMat[0][1] = 2.0f * (q1q2 + -q0q3);
    mahony_filter->rMat[0][2] = 2.0f * (q1q3 - -q0q2);

    mahony_filter->rMat[1][0] = 2.0f * (q1q2 - -q0q3);
    mahony_filter->rMat[1][1] = 1.0f - 2.0f * q1q1 - 2.0f * q3q3;
    mahony_filter->rMat[1][2] = 2.0f * (q2q3 + -q0q1);

    mahony_filter->rMat[2][0] = 2.0f * (q1q3 + -q0q2);
    mahony_filter->rMat[2][1] = 2.0f * (q2q3 - -q0q1);
    mahony_filter->rMat[2][2] = 1.0f - 2.0f * q1q1 - 2.0f * q2q2;
}
```

## 3.3 由旋转矩阵求解出欧拉角

常见的Rotation Matrix，按XYZ旋转顺序的公式如下：

$$
R(\alpha,\beta,\gamma) = 
\begin{bmatrix}
\cos(\beta)\cos(\gamma) & -\cos(\alpha)\sin(\gamma) + \sin(\alpha)\sin(\beta)\cos(\gamma) & \sin(\alpha)\sin(\gamma) + \cos(\alpha)\sin(\beta)\cos(\gamma) \\
\cos(\beta)\sin(\gamma) & \cos(\alpha)\cos(\gamma) + \sin(\alpha)\sin(\beta)\sin(\gamma) & -\sin(\alpha)\cos(\gamma) + \cos(\alpha)\sin(\beta)\sin(\gamma) \\
-\sin(\beta) & \sin(\alpha)\cos(\beta) & \cos(\alpha)\cos(\beta)
\end{bmatrix}
$$

旋转矩阵 Rotation Matrix 和欧拉角 (α,β,γ) 之间也可相互转换。例如，对于 XYZ旋转顺序（首先绕 X 轴，然后绕 Y 轴，最后绕 Z 轴）：

<img src="https://cdn.jsdelivr.net/gh/lcekold/blogimage@main/Network/20260322014119842.png">

其中：

1. $q_0 ,\ q_1 ,\ q_2 ,\ q_3 $为前面由角速度求得的四元数。
2. r(roll),p(pitch),y(yaw)分别为滚转角，俯仰角，偏航角

至此，我们通过角速度求得欧拉角。

旋转矩阵转欧拉角的代码如下：

```c
// 从旋转矩阵中提取姿态角
void mahony_output(struct MAHONY_FILTER_t *mahony_filter)
{
    /*计算roll pitch yaw 欧拉角*/
    mahony_filter->pitch = -asinf(mahony_filter->rMat[2][0]) * RAD2DEG; 
    mahony_filter->roll = atan2f(mahony_filter->rMat[2][1], mahony_filter->rMat[2][2]) * RAD2DEG;
    mahony_filter->yaw = atan2f(mahony_filter->rMat[1][0], mahony_filter->rMat[0][0]) * RAD2DEG;
}
```

# 四、由加速度求欧拉角

由加速度求欧拉角，原理上是根据各轴加速度（重力加速度在各轴的投影）来求解各个角度，这里可以通过绕各个轴旋转（按ZYX方式）求出各个轴加速度，再进一步求取各个角度。

加速度传感器获取到的加速度数据可以用来求解欧拉角中的俯仰角和滚转角。这里直接给出结论，推导过程省略。

<div align="center">
<img src="https://cdn.jsdelivr.net/gh/lcekold/blogimage@main/Network/20260322014610690.png"></div>

由于绕Z轴旋转时，感受到的加速度（重力加速度分量）是不变的，因此，由加速度无法计算偏航角（yaw）。通过对上式求解可得：

<div><img src="https://cdn.jsdelivr.net/gh/lcekold/blogimage@main/Network/20260322014725009.png"></div>

其中：

1. $a_x ,\ a_y ,\ a_z $分别为加速度传感器获取到的绕x轴、y轴、z轴的加速度值
2. r(roll),p(pitch)分别为滚转角，俯仰角

至此，我们通过加速度求得欧拉角（除偏航角（yaw））。

# 五、进行数据融合

只有角速度的积分得到的欧拉角会随着时间的推移而产生漂移（drift），这是因为陀螺仪测量的角速度存在噪声和零偏（bias）。

而加速度计在静止或匀速运动时可以准确测量重力加速度的分量，从而提供一个稳定的参考来校正陀螺仪的漂移。因此，通过将陀螺仪的角速度积分得到的欧拉角与加速度计测量的欧拉角进行融合，可以有效地减小漂移，提高姿态解算的准确性。

<div align="center">
    <img src="https://cdn.jsdelivr.net/gh/lcekold/blogimage@main/Network/20260322015450687.png">
</div>

框图中所有符号的物理意义：

| 符号 | 物理含义 |
| --- | --- |
| $\Omega^b$ | 陀螺仪输出的、载体坐标系（b 系）下的三轴角速度测量值，单位为 `rad/s`，已做基础零偏校准 |
| $f^b$ | 加速度计输出的、b 系下的三轴比力，也就是加速度测量值，单位为 `m/s^2`；静止时它等于 b 系下的重力矢量 |
| $V_{\text{air}}^b$ | 空速计输出的 b 系下空速矢量，是可选模块，通常仅在固定翼等高速平动场景中使用 |
| $\hat{g}^b$ | 加速度计测量并归一化后的 b 系重力单位矢量 |
| $\hat{v}$ | 由当前姿态四元数计算出的理论 b 系重力单位矢量，也就是将导航系中的重力矢量旋转到 b 系后的结果 |
| $e$ | 姿态误差，由测量重力矢量和理论重力矢量做叉乘得到 |
| $K_P + \dfrac{K_I}{s}$ | PI 控制器，即比例项加积分项，用于校正姿态误差并补偿陀螺仪零偏 |
| $\hat{\Omega}$ | 经误差校正后的最终角速度，用于姿态更新 |
| $\dot{q} = \dfrac{1}{2} q \otimes p(\hat{\Omega})$ | 四元数微分方程，用于更新姿态四元数，其中 $\otimes$ 表示四元数哈密顿乘积 |
| Quaternion | 输出的单位姿态四元数，可直接进一步转换为欧拉角 |

过程如下：

## 5.1 计算积分带来的误差

通过计算测量重力矢量 $\hat{g}^b$ 和理论重力矢量 $\hat{v}$ 之间的误差 $e$，可以得到当前姿态与实际姿态之间的偏差。

加速度计的重力加速度矢量是：

$$
\mathbf{a} = 
\begin{bmatrix}
a_x \\
a_y \\
a_z
\end{bmatrix}
$$

归一化后得到单位重力矢量：
$$
\hat{\mathbf{g}} = \frac{\mathbf{a}}{||\mathbf{a}||}
$$

陀螺仪积分出来的重力加速度矢量是：

$$
\mathbf{g} = 
\begin{bmatrix}
g_x \\
g_y \\
g_z 
\end{bmatrix} = \boldsymbol{R} \cdot 
\begin{bmatrix}0 \\0 \\1\end{bmatrix} = \begin{bmatrix}R_{20} \\ R_{21} \\ R_{22}\end{bmatrix}
$$

归一化后的理论重力矢量：
$$\hat{\mathbf{v}} = \frac{\mathbf{g}}{||\mathbf{g}||}$$

归一化的主要目的是为了消除加速度的量级影响，使得误差计算更关注方向上的偏差。

单位化后进行叉乘
$$
\mathbf{e} = sin(\theta) =\hat{\mathbf{g}} \times \hat{\mathbf{a}}
$$

按照国际惯例，当误差角度较小时，sin(θ)≈θ.

通过叉乘计算得到的误差代码如下：

```c
/*单位化加速计测量值*/
    normalise = invSqrt(mahony_filter->acc.x * mahony_filter->acc.x + mahony_filter->acc.y * mahony_filter->acc.y + mahony_filter->acc.z * mahony_filter->acc.z);
    mahony_filter->acc.x *= normalise;
    mahony_filter->acc.y *= normalise;
    mahony_filter->acc.z *= normalise;

    /*加速计读取的方向与重力加速计方向的差值，用向量叉乘计算*/
    ex = (mahony_filter->acc.y * mahony_filter->rMat[2][2] - mahony_filter->acc.z * mahony_filter->rMat[2][1]);
    ey = (mahony_filter->acc.z * mahony_filter->rMat[2][0] - mahony_filter->acc.x * mahony_filter->rMat[2][2]);
    ez = (mahony_filter->acc.x * mahony_filter->rMat[2][1] - mahony_filter->acc.y * mahony_filter->rMat[2][0]);
```

## 5.2 使用PI控制器修正误差

PID中的PI控制器可以看作一个低通滤波器，因为它对误差进行积分，能够平滑掉高频噪声，同时对持续存在的误差进行补偿。通过调整比例增益 $K_P$ 和积分增益 $K_I$，可以控制系统的响应速度和稳定性。它用于整合加速度计的低频信息并与陀螺仪读数互补，从而实现互补滤波的效果。

PI控制器修正误差的代码如下：

```c
 /*误差累计，与积分常数相乘*/
    mahony_filter->exInt += mahony_filter->Ki * ex * mahony_filter->dt ;  
    mahony_filter->eyInt += mahony_filter->Ki * ey * mahony_filter->dt ;
    mahony_filter->ezInt += mahony_filter->Ki * ez * mahony_filter->dt ;
    
    /*用叉积误差来做PI修正陀螺零偏，即抵消陀螺读数中的偏移量*/
    mahony_filter->gyro.x += mahony_filter->Kp * ex + mahony_filter->exInt;
    mahony_filter->gyro.y += mahony_filter->Kp * ey + mahony_filter->eyInt;
    mahony_filter->gyro.z += mahony_filter->Kp * ez + mahony_filter->ezInt;
```

## 5.3 用修正后的角速度重新积分，得到真实的姿态角


# 六、完整代码

```c
#ifndef _MAHONY_FILTER_H
#define _MAHONY_FILTER_H

#include "Tiger_math.h"

/*************************************
完成时间：2023年09月02日 
功能介绍：实现mahony姿态角解算算法的模块封装
知乎账号：龙胆也
B站账号：华南小虎队
***************************************/

struct MAHONY_FILTER_t;

// 定义 MAHONY_FILTER_t 结构体，用于封装 Mahony 滤波器的数据和函数
struct MAHONY_FILTER_t
{
    // 输入参数
    float Kp, Ki;          // 比例和积分增益
    float dt;              // 采样时间间隔
    Axis3f  gyro, acc;     // 陀螺仪和加速度计数据

    // 过程参数
    float exInt, eyInt, ezInt;                // 积分误差累计
    float q0, q1, q2, q3;            // 四元数
    float rMat[3][3];               // 旋转矩阵

    // 输出参数
    float pitch, roll, yaw;         // 姿态角：俯仰角，滚转角，偏航角

    // 函数指针
    void (*mahony_init)(struct MAHONY_FILTER_t *mahony_filter, float Kp, float Ki, float dt);
    void (*mahony_input)(struct MAHONY_FILTER_t *mahony_filter, Axis3f gyro, Axis3f acc);
    void (*mahony_update)(struct MAHONY_FILTER_t *mahony_filter);
    void (*mahony_output)(struct MAHONY_FILTER_t *mahony_filter);
    void (*RotationMatrix_update)(struct MAHONY_FILTER_t *mahony_filter);
};

// 函数声明
void mahony_init(struct MAHONY_FILTER_t *mahony_filter, float Kp, float Ki, float dt);          // 初始化函数
void mahony_input(struct MAHONY_FILTER_t *mahony_filter, Axis3f gyro, Axis3f acc);              // 输入数据函数
void mahony_update(struct MAHONY_FILTER_t *mahony_filter);                                      // 更新滤波器函数
void mahony_output(struct MAHONY_FILTER_t *mahony_filter);                                      // 输出姿态角函数
void RotationMatrix_update(struct MAHONY_FILTER_t *mahony_filter);                              // 更新旋转矩阵函数

#endif
```

```c
#include "mahony_filter.h"

/*************************************
完成时间：2023年09月02日 
功能介绍：实现mahony姿态角解算算法的模块封装
知乎账号：龙胆也
B站账号：华南小虎队
***************************************/

struct MAHONY_FILTER_t mahony_filter;

/*计算旋转矩阵*/
void RotationMatrix_update(struct MAHONY_FILTER_t *mahony_filter)
{
    float q1q1 = mahony_filter->q1 * mahony_filter->q1;
    float q2q2 = mahony_filter->q2 * mahony_filter->q2;
    float q3q3 = mahony_filter->q3 * mahony_filter->q3;

    float q0q1 = mahony_filter->q0 * mahony_filter->q1;
    float q0q2 = mahony_filter->q0 * mahony_filter->q2;
    float q0q3 = mahony_filter->q0 * mahony_filter->q3;
    float q1q2 = mahony_filter->q1 * mahony_filter->q2;
    float q1q3 = mahony_filter->q1 * mahony_filter->q3;
    float q2q3 = mahony_filter->q2 * mahony_filter->q3;

    mahony_filter->rMat[0][0] = 1.0f - 2.0f * q2q2 - 2.0f * q3q3;
    mahony_filter->rMat[0][1] = 2.0f * (q1q2 + -q0q3);
    mahony_filter->rMat[0][2] = 2.0f * (q1q3 - -q0q2);

    mahony_filter->rMat[1][0] = 2.0f * (q1q2 - -q0q3);
    mahony_filter->rMat[1][1] = 1.0f - 2.0f * q1q1 - 2.0f * q3q3;
    mahony_filter->rMat[1][2] = 2.0f * (q2q3 + -q0q1);

    mahony_filter->rMat[2][0] = 2.0f * (q1q3 + -q0q2);
    mahony_filter->rMat[2][1] = 2.0f * (q2q3 - -q0q1);
    mahony_filter->rMat[2][2] = 1.0f - 2.0f * q1q1 - 2.0f * q2q2;
}

// 将陀螺仪和加速度计的输入值赋予滤波器结构体
void mahony_input(struct MAHONY_FILTER_t *mahony_filter,Axis3f gyro,Axis3f acc)
{
    mahony_filter->gyro = gyro;
    mahony_filter->acc = acc;
}

// 使用Mahony算法更新滤波器状态
void mahony_update(struct MAHONY_FILTER_t *mahony_filter)
{
    float normalise;
    float ex,ey,ez;
    
    /*角速度，度转弧度*/
    mahony_filter->gyro.x *= DEG2RAD;        /* 度转弧度 */
    mahony_filter->gyro.y *= DEG2RAD;
    mahony_filter->gyro.z *= DEG2RAD;      
    /*单位化加速计测量值*/
    normalise = invSqrt(mahony_filter->acc.x * mahony_filter->acc.x + mahony_filter->acc.y * mahony_filter->acc.y + mahony_filter->acc.z * mahony_filter->acc.z);
    mahony_filter->acc.x *= normalise;
    mahony_filter->acc.y *= normalise;
    mahony_filter->acc.z *= normalise;

    /*加速计读取的方向与重力加速计方向的差值，用向量叉乘计算*/
    ex = (mahony_filter->acc.y * mahony_filter->rMat[2][2] - mahony_filter->acc.z * mahony_filter->rMat[2][1]);
    ey = (mahony_filter->acc.z * mahony_filter->rMat[2][0] - mahony_filter->acc.x * mahony_filter->rMat[2][2]);
    ez = (mahony_filter->acc.x * mahony_filter->rMat[2][1] - mahony_filter->acc.y * mahony_filter->rMat[2][0]);
    
    /*误差累计，与积分常数相乘*/
    mahony_filter->exInt += mahony_filter->Ki * ex * mahony_filter->dt ;  
    mahony_filter->eyInt += mahony_filter->Ki * ey * mahony_filter->dt ;
    mahony_filter->ezInt += mahony_filter->Ki * ez * mahony_filter->dt ;
    
    /*用叉积误差来做PI修正陀螺零偏，即抵消陀螺读数中的偏移量*/
    mahony_filter->gyro.x += mahony_filter->Kp * ex + mahony_filter->exInt;
    mahony_filter->gyro.y += mahony_filter->Kp * ey + mahony_filter->eyInt;
    mahony_filter->gyro.z += mahony_filter->Kp * ez + mahony_filter->ezInt;
    
    /* 一阶近似算法，四元数运动学方程的离散化形式和积分 */
    float q0Last = mahony_filter->q0;
    float q1Last = mahony_filter->q1;
    float q2Last = mahony_filter->q2;
    float q3Last = mahony_filter->q3;
    float halfT = mahony_filter->dt * 0.5f;
    mahony_filter->q0 += (-q1Last * mahony_filter->gyro.x - q2Last * mahony_filter->gyro.y - q3Last * mahony_filter->gyro.z) * halfT;
    mahony_filter->q1 += ( q0Last * mahony_filter->gyro.x + q2Last * mahony_filter->gyro.z - q3Last * mahony_filter->gyro.y) * halfT;
    mahony_filter->q2 += ( q0Last * mahony_filter->gyro.y - q1Last * mahony_filter->gyro.z + q3Last * mahony_filter->gyro.x) * halfT;
    mahony_filter->q3 += ( q0Last * mahony_filter->gyro.z + q1Last * mahony_filter->gyro.y - q2Last * mahony_filter->gyro.x) * halfT;
    
    /*单位化四元数*/
    normalise = invSqrt(mahony_filter->q0 * mahony_filter->q0 + 
                        mahony_filter->q1 * mahony_filter->q1 + 
                        mahony_filter->q2 * mahony_filter->q2 + 
                        mahony_filter->q3 * mahony_filter->q3);
    mahony_filter->q0 *= normalise;
    mahony_filter->q1 *= normalise;
    mahony_filter->q2 *= normalise;
    mahony_filter->q3 *= normalise;
    
    /*计算旋转矩阵*/
    mahony_filter->RotationMatrix_update(mahony_filter);
}

// 从旋转矩阵中提取姿态角
void mahony_output(struct MAHONY_FILTER_t *mahony_filter)
{
    /*计算roll pitch yaw 欧拉角*/
    mahony_filter->pitch = -asinf(mahony_filter->rMat[2][0]) * RAD2DEG; 
    mahony_filter->roll = atan2f(mahony_filter->rMat[2][1], mahony_filter->rMat[2][2]) * RAD2DEG;
    mahony_filter->yaw = atan2f(mahony_filter->rMat[1][0], mahony_filter->rMat[0][0]) * RAD2DEG;
}

// 初始化Mahony滤波器的参数和函数指针
void mahony_init(struct MAHONY_FILTER_t *mahony_filter,float Kp,float Ki,float dt)
{
    mahony_filter->Kp = Kp;
    mahony_filter->Ki = Ki;
    mahony_filter->dt = dt;
    mahony_filter->q0 = 1;
    mahony_filter->mahony_input = mahony_input;
    mahony_filter->mahony_update = mahony_update;
    mahony_filter->mahony_output = mahony_output;    
    mahony_filter->RotationMatrix_update = RotationMatrix_update;
}
```

调用：

```c
// 全局滤波器对象，你的代码里已经有了
extern struct MAHONY_FILTER_t mahony_filter;

void IMU_Init(void)
{
    // 例子：采样周期 1ms，先从 Kp=2.0, Ki=0.0 开始调
    mahony_init(&mahony_filter, 2.0f, 0.0f, 0.001f);

    // 建议补这一句，先根据初始四元数生成一次旋转矩阵
    mahony_filter.RotationMatrix_update(&mahony_filter);
}

void IMU_Task_1ms(void)
{
    Axis3f gyro;
    Axis3f acc;

    // 这里填你自己的传感器数据
    // gyro 单位必须是 deg/s
    gyro.x = gyro_x_dps;
    gyro.y = gyro_y_dps;
    gyro.z = gyro_z_dps;

    // acc 可以是 g、m/s^2，甚至是标定后的原始值
    // 因为 update 里会做归一化
    acc.x = acc_x;
    acc.y = acc_y;
    acc.z = acc_z;

    mahony_filter.mahony_input(&mahony_filter, gyro, acc);
    mahony_filter.mahony_update(&mahony_filter);
    mahony_filter.mahony_output(&mahony_filter);

    printf("roll=%.2f, pitch=%.2f, yaw=%.2f\r\n",
           mahony_filter.roll,
           mahony_filter.pitch,
           mahony_filter.yaw);
}
```